# SageMaker / Notebook install — same as requirements.txt but without tiktoken.
# tiktoken often fails to compile on older Notebook Instance AMIs (needs Rust/GCC 9+).
# The notebook install cell tries a pre-built wheel or conda; the app works without it.

openai>=1.30.0
uvloop>=0.19.0
pydantic>=2.7.0
pydantic-settings>=2.3.0
tenacity>=8.3.0
numpy>=1.26.0,<2.0.0
pandas>=2.2.0,<3.0.0
click>=8.1.0
tqdm>=4.66.0
python-dotenv>=1.0.0
