# ============================================================
# Mobile Device Collector — Python Dependencies
# Install: pip install -r requirements.txt
# Python 3.11+
# ============================================================

# Core LLM
openai>=1.30.0

# Faster event loop on Linux/macOS (SageMaker runs Amazon Linux — this helps)
uvloop>=0.19.0

# Data validation
pydantic>=2.7.0
pydantic-settings>=2.3.0

# Retry logic
tenacity>=8.3.0

# Data processing & output
# Pin numpy<2 on older SageMaker AMIs (GCC 7.x): numpy 2.x may fall back to
# source builds that require GCC >= 9.3. Wheels for 1.26.x work on AL2.
numpy>=1.26.0,<2.0.0
pandas>=2.2.0,<3.0.0

# Token counting — optional on SageMaker AMI (see Run_Device_Collector.ipynb install cell)
# App falls back to character-based estimates if tiktoken is not installed.
tiktoken>=0.7.0

# CLI
click>=8.1.0

# Progress bar
tqdm>=4.66.0

# Environment loading
python-dotenv>=1.0.0

# ---- Dev / Test dependencies --------------------------------
# Install with: pip install -r requirements.txt -r requirements-dev.txt

# (split into requirements-dev.txt for cleaner production installs)
